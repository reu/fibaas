# Fibbas (NextJS)

### Install and Run
```bash
yarn install
yarn build
yarn start
```

Test connection:
```bash
curl http://localhost:4000/10
```

Benchmark with:
```bash
wrk -c 64 -d 30s http://localhost:4000/10
```
